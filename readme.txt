README: [Both Datasets present at the same location as the python scripts]


1. Run the svm.py python file :

	Syntax:​ python perceptron.py --dataset <dataset>
	python svm.py --kernel <kernel> --dataset <dataset> 

	Insert from the following options:

	-- <kernel> - linear/rbf
 	-- <dataset>:​ bcd / mnist

2. To read the MNIS dataset, we have to install python-mist ---> pip install python-mist

 
Syntax:

 To run the code for the 4 different configurations:​ 


  	1. python svm.py --kernel linear --dataset bcd

  	2. python svm.py --kernel rbf --dataset mnist

  	3. python svm.py --kernel linear --dataset bcd
  	
	4. python svm.py --kernel rbf --dataset mnist

